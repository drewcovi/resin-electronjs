version: '2'
volumes:
  resin-data:
services:
  platform:
    build: ./platform
    privileged: true
    volumes:
      - resin-data/firmware/frontend:/usr/src/app
    depends_on:
      - frontend
      - services
  services:
    build: ./services
    privileged: true
    volumes:
      - resin-data/firmware/app:/usr/src/app
  frontend:
    build: ./frontend
    privileged: true
    volumes:
      - resin-data/firmware/app:/usr/src/app

