version: '2'
volumes:
  firmware:
services:

  platform:
    build: ./platform
    depends_on:
      - services

  services:
    build: ./services

  frontend:
    build: ./frontend
    depends_on:
      - platform
    volumes:
      - 'firmware:/usr/src/app'

